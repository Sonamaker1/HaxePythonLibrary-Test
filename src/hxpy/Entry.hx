package hxpy;

import hxpy.Api;
import hxpy.ComplexData;
import hxpy.KV;
import hxpy.Person;

// This can be empty-ish; it just gives Haxe a main entry point.
class Entry {
  public static function main() {
    // no-op
    var complexData = Api.buildComplex();
  }
}
